name: Build Equilibrium (CI)

on:
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
            target/
          key: ${{ runner.os }}-equilibrium-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-equilibrium-

      - name: Install Linux deps
        if: runner.os == 'Linux'
        run: sudo apt-get update -y && sudo apt-get install -y pkg-config libfreetype6-dev libfontconfig1-dev libgles2-mesa-dev libx11-dev libx11-xcb-dev libxcb1-dev libxcursor-dev libxi-dev libxrandr-dev libwayland-dev libxkbcommon-dev libegl-dev libgl-dev

      - name: Install cargo-truce
        run: cargo install --git https://github.com/truce-audio/truce cargo-truce

      - name: Build Equilibrium
        run: cargo truce build --clap -p equilibrium

      - name: Read version
        id: version
        shell: bash
        run: echo "version=$(grep '^version' plugins/equilibrium/Cargo.toml | head -1 | sed 's/.*\"\(.*\)\"/\1/')" >> $GITHUB_OUTPUT

      - name: Package (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          cp -r target/bundles/Equilibrium.clap dist/
          echo "Equilibrium v${{ steps.version.outputs.version }} — ${{ runner.os }}"

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item target\bundles\Equilibrium.clap dist\
          Write-Host "Equilibrium v${{ steps.version.outputs.version }} — Windows"

      - uses: actions/upload-artifact@v6
        with:
          name: Equilibrium-v${{ steps.version.outputs.version }}-${{ runner.os == 'Linux' && 'linux' || (runner.os == 'macOS' && 'mac' || 'win') }}
          path: dist/
          if-no-files-found: error
